import React, {Component} from 'react';
import {View, Text, TouchableHighlight, TouchableOpacity} from 'react-native';

const CustomDrawingScreen = props => {
  const ref = React.useRef(null);
  return (
    <View style={{flex: 1}}>
      <TouchableOpacity>
        <Text>assda</Text>
      </TouchableOpacity>
    </View>
  );
};
export default CustomDrawingScreen;
